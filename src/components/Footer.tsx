/**
 * Shared site-wide footer. Re-exports the landing page `FooterSection` so every
 * public page (pricing, help, legal, etc.) inherits the same dark brand canvas.
 */
export { FooterSection as Footer } from "@/components/landing/FooterSection";
