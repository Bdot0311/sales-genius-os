import { DashboardLayout } from "@/components/dashboard/DashboardLayout";
import { SequencesList } from "@/components/sequences";

const Sequences = () => {
  return (
    <DashboardLayout>
      <SequencesList />
    </DashboardLayout>
  );
};

export default Sequences;
